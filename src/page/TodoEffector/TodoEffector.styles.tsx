import styled from '@emotion/styled';

export const TodoEffectorRoot = styled('div')({
  width: '100%',
  height: '100vh',
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
});
