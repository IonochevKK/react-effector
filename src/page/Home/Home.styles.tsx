import styled from '@emotion/styled';

export const HomeRoot = styled('div')({
  width: '100%',
  height: '100vh',
  display: 'flex',
  justifyContent: 'center',
});

export const HomeWrapperButtons = styled('div')({
  padding: '40px',
});
